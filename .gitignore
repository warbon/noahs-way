node_modules
.next
out
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
tsconfig.tsbuildinfo
.vercel
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store

certificates
.env*
# ...but the template carries no secrets and documents required config.
!.env.example

# Customer inquiry submissions (PII) — never commit
/data/inquiries.json

# Admin-managed assistant settings (encrypted API keys) — never commit
/data/ai-settings.json

# Throwaway scripts run from the project root so they can resolve node_modules.
.tmp-*

# Admin-managed Facebook settings (encrypted Page token) — never commit
/data/facebook-settings.json
