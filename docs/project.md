# 🌍 Noah’s Way Travel – Phase 1 Implementation Guide

We are focusing ONLY on Phase 1:

✅ Landing Page  
✅ Next.js 14 App Router  
✅ Tailwind CSS  
✅ shadcn/ui components  
❌ No database yet  
❌ No backend logic yet  

This is the clean, working baseline.

---

# 1️⃣ Create the Project

```bash
npx create-next-app@latest noahs-way
```

Select:
- ✔ TypeScript
- ✔ Tailwind CSS
- ✔ App Router
- ✔ ESLint
- ✔ src directory (optional but recommended)

Then:

```bash
cd noahs-way-travel
npm run dev
```

Confirm it runs at:
http://localhost:3000

---

# 2️⃣ Install shadcn/ui

Initialize:

```bash
npx shadcn-ui@latest init
```

Choose:
- ✔ TypeScript
- ✔ App Router
- ✔ Default style

Then install components:

```bash
npx shadcn-ui@latest add button
npx shadcn-ui@latest add card
npx shadcn-ui@latest add input
npx shadcn-ui@latest add textarea
```

---

# 3️⃣ Final Clean Project Structure

```
app/
│
├── layout.tsx
├── page.tsx
├── globals.css
│
components/
│
├── Navbar.tsx
├── Hero.tsx
├── Packages.tsx
├── Contact.tsx
├── Footer.tsx
│
components/ui/   (auto-generated by shadcn)
```

---

# 4️⃣ layout.tsx

```tsx
import "./globals.css"

export const metadata = {
  title: "Noah's Way Travel",
  description: "Forget the Map – Travel Beyond Boundaries",
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="bg-background text-foreground">
        {children}
      </body>
    </html>
  )
}
```

---

# 5️⃣ Homepage (app/page.tsx)

```tsx
import Navbar from "@/components/Navbar"
import Hero from "@/components/Hero"
import Packages from "@/components/Packages"
import Contact from "@/components/Contact"
import Footer from "@/components/Footer"

export default function Home() {
  return (
    <main>
      <Navbar />
      <Hero />
      <Packages />
      <Contact />
      <Footer />
    </main>
  )
}
```

---

# 6️⃣ Navbar Component

```tsx
"use client"

import { Button } from "@/components/ui/button"

export default function Navbar() {
  return (
    <header className="flex items-center justify-between px-8 py-4 border-b">
      <h1 className="text-2xl font-bold">Noah's Way Travel</h1>
      <nav className="hidden md:flex gap-6 text-sm">
        <a href="#packages">Packages</a>
        <a href="#contact">Contact</a>
      </nav>
      <Button>Book Now</Button>
    </header>
  )
}
```

---

# 7️⃣ Hero Section

```tsx
"use client"

import { Button } from "@/components/ui/button"

export default function Hero() {
  return (
    <section className="grid md:grid-cols-2 gap-10 items-center px-8 py-20">
      <div>
        <h2 className="text-4xl md:text-5xl font-extrabold">
          Forget the Map.
        </h2>
        <p className="mt-4 text-muted-foreground">
          Discover curated travel experiences to Singapore, Japan, Korea, and beyond.
        </p>
        <div className="mt-6 flex gap-4">
          <Button size="lg">Explore Packages</Button>
          <Button variant="outline" size="lg">Get a Quote</Button>
        </div>
      </div>

      <div className="bg-muted rounded-2xl h-80 shadow-lg" />
    </section>
  )
}
```

---

# 8️⃣ Packages Section

```tsx
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card"

export default function Packages() {
  return (
    <section id="packages" className="px-8 py-20 bg-muted/40">
      <h3 className="text-3xl font-bold text-center mb-12">Popular Packages</h3>
      <div className="grid md:grid-cols-3 gap-6">
        <Card className="rounded-2xl shadow-md">
          <CardHeader>
            <CardTitle>Singapore Adventure</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-muted-foreground">
              4 Days / 3 Nights • Hotel • Tours • Transfers
            </p>
          </CardContent>
        </Card>
      </div>
    </section>
  )
}
```

---

# 9️⃣ Contact Section

```tsx
import { Input } from "@/components/ui/input"
import { Textarea } from "@/components/ui/textarea"
import { Button } from "@/components/ui/button"

export default function Contact() {
  return (
    <section id="contact" className="px-8 py-20">
      <div className="max-w-xl mx-auto">
        <h3 className="text-3xl font-bold text-center mb-8">Request a Quote</h3>
        <div className="space-y-4">
          <Input placeholder="Full Name" />
          <Input type="email" placeholder="Email Address" />
          <Textarea placeholder="Destination & Travel Dates" />
          <Button className="w-full">Send Inquiry</Button>
        </div>
      </div>
    </section>
  )
}
```

---

# 🔟 Footer

```tsx
export default function Footer() {
  return (
    <footer className="text-center py-6 border-t text-sm text-muted-foreground">
      © {new Date().getFullYear()} Noah's Way Travel & Tours. All rights reserved.
    </footer>
  )
}
```

---

# ✅ Phase 1 Complete

You now have:

✔ Clean Next.js 14 structure  
✔ shadcn components  
✔ Responsive landing page  
✔ Production-ready foundation

---

When you're ready, we move to Phase 2:
- Dynamic packages
- Database
- Inquiry saving
- Admin dashboard
- AI automation

For now, run:

```bash
npm run dev
```

and confirm everything renders correctly.
